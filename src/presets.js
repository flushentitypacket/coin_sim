export const bitcoin = {
  blockSize: 1,
  blockDifficulty: 10 * 60,
}

export const bitcoinCash = {
  blockSize: 8,
  blockDifficulty: 10 * 60,
}

export const litecoin = {
  blockSize: 1,
  blockDifficulty: 2.5 * 60,
}
